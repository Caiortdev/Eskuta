{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissões da janela principal do Eskuta. O sidecar Python é spawnado via std::process::Command direto do Rust (não precisa de shell capability).",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "updater:default"]
}
