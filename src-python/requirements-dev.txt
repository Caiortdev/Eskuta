# ============================================================
# Eskuta — Sidecar Python (deps de desenvolvimento e testes)
# Instalar com: pip install -r requirements.txt -r requirements-dev.txt
# ============================================================

# --- Testes ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
# pytest-benchmark: usado em tests/benchmarks/ (Fase 1.9.5 / Bloco A.4)
# Rodar localmente: pytest tests/benchmarks/ --benchmark-only
pytest-benchmark==5.2.3

# --- Lint e auditoria ---
ruff==0.6.9
pip-audit==2.7.3

# --- Git hooks multi-linguagem ---
# Após instalar este requirements, rode `pre-commit install` na raiz do
# repo pra ativar os hooks de `.pre-commit-config.yaml`.
pre-commit==4.0.1
