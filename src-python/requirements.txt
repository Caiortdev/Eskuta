# ============================================================
# Eskuta — Sidecar Python (FastAPI)
# Versões fixadas conforme RELATORIO_TECNICO.md §0.5
# ============================================================

# --- API framework ---
# Nota: versões originais do RELATORIO_TECNICO (fastapi 0.115.0, starlette
# 0.38.6, python-multipart 0.0.12) tinham CVEs (GHSA-f96h-pmfr-66vw,
# GHSA-2c2j-9gv5-cj73, GHSA-59g5-xgcq-4qw3, etc). Atualizado na auditoria
# da Fase 0 — ver docs/AUDIT-FASE-0.md.
fastapi==0.136.1
starlette==1.0.0
uvicorn[standard]==0.32.0
python-multipart==0.0.29

# --- Validação e settings ---
pydantic==2.9.0
pydantic-settings==2.5.0

# --- HTTP client (chamadas a APIs externas) ---
# 0.28.1 (não 0.27.0 do relatório) — google-genai requer >=0.28.
httpx==0.28.1

# --- Banco de dados (Etapa 1.2) ---
sqlalchemy==2.0.35
aiosqlite==0.20.0
alembic==1.13.3

# --- Observabilidade ---
loguru==0.7.2

# --- Armazenamento seguro de API keys (Etapa 1.11) ---
# Delega pro OS keyring nativo (Credential Manager / Keychain / Secret Service).
keyring==25.4.1

# --- Pipeline de áudio (Etapa 1.3) ---
# ffmpeg-python: wrapper Python sobre o binário ffmpeg (precisa estar no PATH).
# silero-vad: modelo leve de Voice Activity Detection (~30MB do torch.hub).
ffmpeg-python==0.2.0
silero-vad==5.1.2

# --- Camada de transcrição (Etapa 1.4) ---
# groq: SDK oficial pro Whisper Large v3 Turbo (provider STT primário).
# assemblyai: SDK oficial do fallback (modelo Universal, suporta pt-BR).
groq==1.2.0
assemblyai==0.64.3

# --- Camada de diarização (Etapa 1.5) ---
# pyannote.audio: identifica QUEM falou quando. Modelo "speaker-diarization-3.1"
# (~500MB) baixa do Hugging Face Hub na primeira execução — precisa HF_TOKEN.
# Versão 4.0.4 (não 3.3.2 do relatório) por compat com torchaudio 2.11+.
pyannote.audio==4.0.4

# --- Camada de LLM (Etapa 1.6) ---
# 3 SDKs oficiais — usuário escolhe qual usar plugando a key dele.
# google-genai (novo) substitui google-generativeai (deprecated em 2024).
anthropic==0.104.0
openai==2.38.0
google-genai==2.5.0

# --- Geração de ata anti-alucinação (Etapa 1.7) ---
# rapidfuzz: validação fuzzy de citações (quote vs transcrição). Versão
# 3.14.5 (não 3.10.0 do relatório) — patch upgrade compat.
rapidfuzz==3.14.5
